<div style="padding: 10px 0;">
    <p>
        <b>{name}</b> is running. It automatically converts every storage, memory and network byte value
        shown to users on the client dashboard from Pterodactyl's default binary units (KiB, MiB, GiB, TiB)
        into decimal units (KB, MB, GB, TB).
    </p>
    <p>
        This extension has no configuration. Admin-side values (server edit, egg configuration, etc.)
        are left untouched and are still entered/displayed in raw MiB.
    </p>
</div>
